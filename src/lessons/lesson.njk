---
layout: layout.njk
pagination:
  data: collections.lessons
  size: 1
  alias: lesson
  addAllPagesToCollections: true
permalink: "lessons/{{ lesson.id }}.html"
eleventyComputed:
  title: "{{ lesson.title }}"
---
<main>
    <div class="container">
      <h1>{{ lesson.title }}</h1>
      <p>{{ lesson.content }}</p>
      <p><strong>Example Code:</strong></p>
      <pre>{{ lesson.exampleCode }}</pre>
      <a href="{{ site.baseUrl }}quizzes/{{ lesson.id }}.html">Take Quiz</a>
    </div>
</main>
